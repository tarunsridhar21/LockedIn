package com.timetrack.app.ui.theme

import androidx.compose.ui.graphics.Color

// ── Light background surfaces ──────────────────────────────────────────────
val LinenBg = Color(0xFFF5F1E8)
val CreamSurface = Color(0xFFFCF8EE)
val CreamContainer = Color(0xFFEFEAE0)
val CreamContainerHigh = Color(0xFFE8E2D5)

// ── Primary — Sage ─────────────────────────────────────────────────────────
val Sage = Color(0xFF7A9971)
val SagePale = Color(0xFFD4DFC8)
val SageDeep = Color(0xFF2E3D29)

// ── Secondary — Lavender ───────────────────────────────────────────────────
val Lavender = Color(0xFFA89BC8)
val LavenderPale = Color(0xFFDFD4F0)

// ── Tertiary — Clay ────────────────────────────────────────────────────────
val Clay = Color(0xFFC99988)
val ClayPale = Color(0xFFF0D4C8)
val ClayDeep = Color(0xFF4F3027)

val SoftError = Color(0xFFB85C50)

// ── Warm neutrals ─────────────────────────────────────────────────────────
val WarmInk = Color(0xFF2E2A22)
val WarmInkMuted = Color(0xFF6E6859)
val WarmInkSubtle = Color(0xFFA8A19A)
val WarmOutline = Color(0xFFD8D2C8)

// ── Dark surfaces — warm midnight ─────────────────────────────────────────
val MidnightBg = Color(0xFF1F1C16)
val MidnightSurface = Color(0xFF2A2620)
val MidnightContainer = Color(0xFF34302A)

val SageLight = Color(0xFFB5CFAE)
val SageContainerDark = Color(0xFF4A6243)

val LavenderLight = Color(0xFFC9BEDF)
val ClayLight = Color(0xFFE8C4B0)

val WarmPaper = Color(0xFFF0EBE0)
val WarmPaperMuted = Color(0xFFC2BCB0)

// ── Timer state colors ─────────────────────────────────────────────────────
val TimerIdleSage = Sage
val TimerRunningClay = Color(0xFFB5806A)
val TimerSavingGold = Color(0xFFD4A574)

// ── Desaturated category palette ──────────────────────────────────────────
val CategorySleeping = Color(0xFFA8B0CE)  // dusty indigo
val CategoryShower = Color(0xFF7FBFB5)    // sea-glass
val CategoryStudying = Color(0xFFD4A574)  // mustard
val CategoryBreak = Color(0xFFB5C9A8)    // sage
val CategoryWorking = Color(0xFF8FAFD0)  // chambray
val CategoryExercise = Color(0xFFC99988) // clay
val CategoryReading = Color(0xFFB8A2D0)  // lavender
val CategoryEating = Color(0xFFD9C384)   // cream gold
val CategoryCommute = Color(0xFF94BFB0)  // mint
